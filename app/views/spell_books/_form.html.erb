<%= form_for(@spell_book) do |f| %>
  <% if @spell_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spell_book.errors.count, "error") %> prohibited this spell_book from being saved:</h2>

      <ul>
      <% @spell_book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :library_id %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
